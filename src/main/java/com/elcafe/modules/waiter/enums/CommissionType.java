package com.elcafe.modules.waiter.enums;

/**
 * Type of commission calculation for a waiter
 */
public enum CommissionType {
    /**
     * Commission is calculated as a percentage of the order total
     */
    PERCENTAGE,

    /**
     * Commission is a fixed amount per order
     */
    FIXED_AMOUNT
}
