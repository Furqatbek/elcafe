package com.elcafe.modules.promotion.enums;

public enum PromotionScope {
    ALL,         // Applies to entire order
    CATEGORY,    // Applies to specific categories
    PRODUCT,     // Applies to specific products
    ORDER_TYPE   // Applies to specific order types (dine-in, takeaway, delivery)
}
